export enum Status {
  Failed,
  Success,
  BadRequest,
  Unauthorized,
  Forbidden,
}

export enum Environment {
  Development = 1,
  Staging = 2,
  QA = 3,
  Testing = 4,
  PreProd = 5,
  Production = 6,
}
